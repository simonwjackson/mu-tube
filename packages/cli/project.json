{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "projectType": "application",
    "sourceRoot": "packages/cli",
    "targets": {
        "serve": {
            "executor": "nx:run-commands",
            "options": {
                "command": "node --experimental-specifier-resolution=node --loader ./loader.js index.ts"
            }
        }
    }
}
